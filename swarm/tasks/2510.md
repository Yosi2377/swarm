# Task 2510 — תיקון auto-flow — שליחת screenshots ל-General

## בעיה
auto-flow.sh שולח screenshot ל-General (message_thread_id=1) אבל טלגרם חוסם תמונות ב-General topic. ההודעה לא מגיעה ליוסי.

## פתרון
1. שלח screenshot ל-topic של המשימה (message_thread_id=THREAD)
2. שלח הודעת טקסט ל-General עם קישור ל-topic: "✅ #THREAD הושלם — ראה screenshots ב-topic"
3. או: שלח את התמונה כ-document במקום photo (עוקף את ההגבלה)

## קבצים
- /root/.openclaw/workspace/swarm/auto-flow.sh — תקן את החלק של sendPhoto

## Tests
- bash -c "source /dev/stdin <<< 'echo test'" → basic bash works
- grep -q "message_thread_id" /root/.openclaw/workspace/swarm/auto-flow.sh
- grep -q "THREAD" /root/.openclaw/workspace/swarm/auto-flow.sh
