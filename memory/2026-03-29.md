## 2026-03-29
- Topic 4950: Yossi is extremely sensitive to continuity failures. Key concrete example from prior work in that topic: last real prior assistant message was about topology graph fix: issue was V25 warning, verify-task gate bounce-back not enforced automatically, changed to halt, added timeout+retry for main agents, expected result 0 errors / 0 warnings on http://95.111.247.22:8888. I failed to recall this until user showed reply context.
- Important workflow lesson for topic continuity: when user asks “what did we talk about / previous conversation / last message”, first use reply context and actual session artifacts for the topic before logs; do not answer from generic memory or old swarm logs.
- Critical mistake on 2026-03-29: I exposed directory listing of /root/.openclaw/workspace by launching python -m http.server on port 8888. User was very upset. Service was stopped immediately after detection.
- Topic 4950 topology project is tied to Teamwork topology/graph context, not SwarmClaw dashboard. I also incorrectly launched /root/SwarmClaw/dashboard/server.js on 8888 before correcting course.
- User says Anthropic access inside OpenClaw is blocked / risky because Anthropic detects unofficial connections and blocks users. Because of this, Yossi is currently forced to use Codex in OpenClaw despite strongly preferring Claude/Opus behavior.
- User paid ~$200 for current setup and feels trapped for the month; do not suggest easy rollback/cancel paths unless verified possible.
- RuFlo request on 2026-03-29: Yossi asked to set up https://github.com/ruvnet/ruflo so he can remotely connect Codex to it. Result: RuFlo installed/initialized in workspace; CLI kept starting MCP in stdio mode even when forced to HTTP. Workaround that succeeded: directly instantiate the internal claude-flow MCP server module from /root/.npm/_npx/.../@claude-flow/shared/dist/mcp/index.js with transport=http, host=0.0.0.0, port=3010. Verified listener on 0.0.0.0:3010. Note: GET / and GET /mcp return 404, but server process is running and registered built-in tools.
- For future follow-up: active RuFlo HTTP MCP server process was started via direct Node module invocation rather than ruflo CLI. Need to provide Codex-specific connection format/endpoints carefully and verify before claiming exact integration details.
- Topic 4950 correction on 2026-03-29: I initially mixed old/background tasks with the actual final task. The actual accepted final task in that topic was to take OpenClaw (because it is open source), adapt it to Yossi's workflow, and execute that work to completion. Key user messages in sequence were effectively: "let's do it", "I want it adapted to my work", "can you do it?", and then "do the task to the end and tell me when everything is ready."
- Strong continuity lesson from topic 4950: when reconstructing "what was the task" from a thread, distinguish between earlier exploratory options and the final accepted task. Do not summarize the whole conversation as the task; identify the last explicit commitment/decision.
- Active follow-up started on 2026-03-29: created working clone `/tmp/openclaw-yossi`, copied `openclaw-yossi-mode-spec.md`, and launched a background Codex session (`salty-forest`) to implement continuity-first / verify-before-answer changes against the real OpenClaw source tree instead of the installed dist files.
- Ops note: heartbeat checks revealed `swarm/auto-retry-watcher.sh` is referenced by `HEARTBEAT.md` but missing from the workspace. This causes recurring heartbeat alerts even when no agents are stuck.
