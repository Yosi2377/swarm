# Task 3365 — סריקת אבטחה מהירה של ZozoBet

## תיאור
בצע בדיקת אבטחה בסיסית:
1. בדוק headers: `curl -I https://zozobet.duckdns.org`
2. בדוק SSL: `echo | openssl s_client -connect zozobet.duckdns.org:443 2>/dev/null | openssl x509 -noout -dates`
3. בדוק פורטים חשופים: `ss -tlnp | grep -E '3001|3002|8089'`
4. בדוק rate limiter: `curl -s https://zozobet.duckdns.org/api/events | head -c 100`

דווח ממצאים עם send.sh shomer 3365 "ממצאים: ..."
