# Task 2633 — חיפוש אינטרנט לפני הסלמה

## בעיה
כשסוכן נכשל 6 פעמים (Phase 1 + Phase 2), המערכת מסלימה ליוסי מיד. אבל לפעמים הפתרון קיים באינטרנט.

## פתרון — הוסף Phase 2.5: Web Search

ב-auto-flow.sh, לפני הסלמה ליוסי (אחרי Phase 2 נכשל):

```bash
## Phase 2.5: Web Search
# 1. חפש באינטרנט את השגיאה
web_fetch "https://www.google.com/search?q=${ERROR_MSG}+solution"
# או: web_search if available

# 2. חפש פרויקטים דומים בGitHub
web_fetch "https://github.com/search?q=${RELEVANT_KEYWORDS}&type=code"

# 3. חפש ב-StackOverflow
web_fetch "https://stackoverflow.com/search?q=${ERROR_MSG}"

# 4. תן לסוכן את מה שנמצא + עוד 2 ניסיונות
send_telegram "$THREAD" "$AGENT" "🔍 מצאתי באינטרנט:
$SEARCH_RESULTS

נסה גישה חדשה בהתבסס על זה!"
```

## גם עדכן SYSTEM.md
הוסף לסוכנים:
```
## 🔍 תקוע? חפש באינטרנט!
אם נכשלת 2 פעמים על אותה שגיאה:
1. web_fetch "https://stackoverflow.com/search?q=ERROR_MESSAGE"
2. web_fetch "https://github.com/search?q=RELEVANT+KEYWORDS&type=code"
3. קרא את התוצאות ונסה גישה חדשה
```

## Tests
- bash -n /root/.openclaw/workspace/swarm/auto-flow.sh
- grep -q "web_fetch\|search\|internet\|google\|stackoverflow" /root/.openclaw/workspace/swarm/auto-flow.sh
- grep -q "חפש\|search\|web" /root/.openclaw/workspace/swarm/SYSTEM.md
