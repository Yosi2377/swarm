# Langfuse — LLM Observability

## Access
- **URL**: http://95.111.247.22:3100
- **First time**: Create account at the URL above

## Docker
```bash
cd /root/langfuse-selfhost
docker compose up -d     # start
docker compose down       # stop
docker compose logs -f    # logs
```

## Memory: ~1.3GB total
## Port: 3100 (host), Redis 6389 (avoiding host Redis on 6379)
