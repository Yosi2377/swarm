- IRC/Ergo setup on 2026-04-05: user connected successfully to Ergo via mIRC after switching from IP to hostname `tworkswarm.duckdns.org`, port `+6697`, SASL enabled, using a real Let's Encrypt TLS certificate instead of the previous self-signed cert.
- User expectation for IRC: Or must appear as a persistent presence in `#myops` and `#agent-chat`, and must answer both in-channel and in private DM exactly like before. All other bot identities should also stay reachable in DM.
- Current unresolved issue: Or/`OrYossiOps8487` is not yet stable on IRC. There was a conflict between a direct OpenClaw IRC connection and a ZNC-based connection, causing nick instability / alternate nick with `_` and unreliable replies. Need future follow-up on ZNC/Ergo/OpenClaw path until Or stays connected persistently and responds in `#myops`, `#agent-chat`, and DM.
- Agent hub state on 2026-04-05: specialized agent nicks were restored/joined to `#myops` and `#agent-chat`; user explicitly wants them visibly connected, not only available in background.
