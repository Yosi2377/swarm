# Archived Cron Jobs (AI-based)
These were disabled on 2026-02-18 because they burned Claude Max tokens.
Each file contains the full cron job config to restore if needed.

To restore: use `cron add` with the job object from each JSON file.
